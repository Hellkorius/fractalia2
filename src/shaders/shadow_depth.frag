#version 450

// Shadow depth fragment shader - minimal fragment shader for depth-only rendering
// No color output needed, just depth testing

void main() {
    // Empty fragment shader for depth-only rendering
    // Depth is automatically written by the rasterizer
    // We could add alpha testing here if needed for transparent objects
}